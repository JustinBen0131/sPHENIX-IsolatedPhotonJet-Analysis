# Python packages for the PhotonID, TreeToHists and FinalAnalysis stages.
# PyROOT (ROOT >= 6.26 with TMVA) comes from the sPHENIX/ROOT installation,
# not from pip.
numpy>=1.24
awkward>=2.0
uproot>=5.0
pyyaml>=6.0
matplotlib>=3.7
# Training only (PhotonID/train_photon_bdt.py):
xgboost>=1.7
scikit-learn>=1.2
